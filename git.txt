git config --global user.name "work_thr"
git config --global user.email "j_home@mail.ru"


…or create a new repository on the command line

echo "# 123" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/rfgtkmrf/py.git
git push -u origin master


…or push an existing repository from the command line

git remote add origin https://github.com/rfgtkmrf/py.git
git push -u origin master